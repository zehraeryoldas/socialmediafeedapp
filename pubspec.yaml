name: socialmediafeedapp
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  firebase_core: ^2.16.0
  flutter:
    sdk: flutter
  cloud_firestore: ^4.9.2
  fluttertoast: ^8.2.2
  image_picker: ^1.0.4
  firebase_storage: ^11.2.7
  firebase_auth: ^4.10.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
